import { FilterReportePipe } from './filter-reporte.pipe';

describe('FilterReportePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportePipe();
    expect(pipe).toBeTruthy();
  });
});
