import { FilterEmpleadoPipe } from './filter-empleado.pipe';

describe('FilterEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
